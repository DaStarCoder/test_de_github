#include <stdlib.h>

float a, b;
Comparaison(a,b){
	if (a<b){
		return a<b;
		printf("Le nombre %f", a "est plus petit que %f", b);
	}else{
		return a>b;
		printf("Le nombre %f", a "est plus grand que %f", b);
	}
}

int main(){

	Comparaison(a,b);
return 0;
}
